# Reference outputs — verification only

These selected tables are optional reference copies of reported results. They exist only so a reproducer can compare newly generated outputs with the submitted results.

They are not inputs to preprocessing, forecasting, clustering, analysis, cohort definition or figure generation. The formal workflow must run successfully if this directory is removed.
